apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: iris-agent-router-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: iris
  source:
    repoURL: https://github.com/YOUR_ORG/IRIS
    targetRevision: HEAD
    path: helm/iris-agent-router
    helm:
      valueFiles:
        - values-dev.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: iris-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  revisionHistoryLimit: 10
