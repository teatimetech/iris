apiVersion: v1
kind: Service
metadata:
  name: {{ include "iris-api-gateway.fullname" . }}
  labels:
    {{- include "iris-api-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iris-api-gateway.selectorLabels" . | nindent 4 }}
