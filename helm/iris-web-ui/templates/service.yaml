apiVersion: v1
kind: Service
metadata:
  name: {{ include "iris-web-ui.fullname" . }}
  labels:
    {{- include "iris-web-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iris-web-ui.selectorLabels" . | nindent 4 }}
