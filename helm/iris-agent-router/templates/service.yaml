apiVersion: v1
kind: Service
metadata:
  name: {{ include "iris-agent-router.fullname" . }}
  labels:
    {{- include "iris-agent-router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iris-agent-router.selectorLabels" . | nindent 4 }}
